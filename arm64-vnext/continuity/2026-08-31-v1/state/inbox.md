# ARM64 vNext Inbox

Epoch: `2026-08-31-v1`  
Last updated: `2026-09-03T07:13:00Z`

This inbox tracks incoming audit reports, newly discovered work, blockers, and decisions. Items are triaged into `plan.md` and the durable `vnext-*` task graph.

Current work appears first. The long sealed runtime-generator narrative below is
retained as historical evidence and is no longer the scheduling view.

## Active reports and handoffs

| Workstream | Session | Expected input | State |
|---|---|---|---|
| Windows on Arm ecosystem debt | Programme owner; no session | Upstream submission decisions | **NEW 2026-09-03.** `state/windows-on-arm-ecosystem-iou-list.md` records NINE items (item 2 re-scoped as unconditional; item 9 added and ordered FIRST). All four live PRs verified OPEN/draft/mergeable but staged in `crutkas/*` forks and **not yet filed upstream**: busybox-w32 #4, build-extra #29, msys2-runtime #31, msys2-woarm64-build #10. Item 1 is the blocking LLVM AArch64 Cygwin TargetInfo, detailed in `state/llvm-aarch64-cygwin-targetinfo-upstream-requirement.md`. Note that binutils, GCC, and Cygwin are mailing-list projects and do not accept GitHub PRs. |
| Runtime ABI session-start request | Frozen V10 owner `e18a4fba-d649-4757-801c-f6560c9ffc13`; terminal first review `797d4709-75a2-4bb3-8b36-b5e482f17a5c`; reviewer of record `8f09cf29-8022-4d43-81c7-f223baaa6143`; authorized ABI session `724ee2e9-51c7-40d7-b303-e6fcbfe78490` | Monitoring only; escalate any hard stop, failed checkout proof, or scope excess | Terminal `GO_RUNTIME_ABI_SESSION_START_LOCAL_BUILD_ONLY`: verdict payload `cca57c2c...`, manifest `c788707a...`, sums `51b7dd74...`, external receipt `cd8452b9...`, independently reverified at 33 read-only files, 31/31 sums, 13/13 seals, zero bytecode. Applying sealed diff `4b6ce05e...` to frozen V9 wrapper `524ada84...` reproduces V10 wrapper `e34cb967...` byte-for-byte; the 145-case suite is 145 PASS with zero compiler/linker children; the 251-case matrix passes across three roots with zero mismatches; 49 uncanned probes show zero deviations. The 48-step/134-postcondition closure was replayed only in declared SYNTHETIC rehearsal mode because frozen `runtime-abi-v9-step-runner.py` line 782 mis-dispatches production argv, so no runtime-product PASS is claimed. `verdict.json` is itself the hash-bound receipt, and the authority is consumed by the single ABI session. |
| Runtime ABI implementation | Sealed session `724ee2e9-51c7-40d7-b303-e6fcbfe78490` | Owner decision on the LP64 TargetInfo path and on commit authority | Bring-up complete at the scope boundary with the hard stop held: HEAD `d890a845...`, 0 commits ahead, 29 files / 785 insertions / 51 deletions uncommitted. AA64 objects 12 to 266 with zero non-AA64, errors 885 to 64, clang crashes 19 to 0, `#error` sites 16 to 1; nothing links and no DLL exists, so no product PASS is claimed. Two sealed backups verified with zero checksum failures (`47059be6...` and `ec458637...`) and the patch passes `git apply --check --reverse` with 29 diff headers. **Restoring requires copying untracked `winsup/cygwin/math/aarch64/longdouble.c` separately, or the tree fails at link.** **SUPERSEDED 2026-09-03:** the premise that "the available toolchain is LLP64" was WRONG. The ecosystem `aarch64-pc-cygwin` GCC target was built and measured as **LP64**, and the 11 LLP64-artifact sites **compile clean under LP64**. The LLVM AArch64 Cygwin `TargetInfo` is still required, but it is now UNCONDITIONAL parity work rather than a data-model gate. |
| Preservation chain validation | `c63ab774-a023-4e57-9bc4-53f727507ada`; re-verified independently by `ee27c231` | None; closed | **CLOSED — recovery validated four ways, read-only, nothing mutated.** 43/43 pre-image blobs match `git rev-parse d890a845:<path>`; forward `git apply --check` on an extracted tree PASSES; reverse-apply PASSES; the diff is pure LF (0 CR bytes) because it was written with `git diff --output=` rather than through a shell pipeline. **`longdouble.c` is REFERENCED BUT NOT CREATED** by the patch (line 187 `Makefile.am` entry; zero `diff --git` headers), which is *why* the separate copy step exists — applying the patch alone fails at LINK. **DISCOVERY HAZARD:** the byte-exact copy lives under `c63ab774` `evidence/untracked/` (3,368 B, LF, `aaf9785b...`, byte-identical to the live tree), while the bundle *named* `arm64-port-backup-2026-09-02` holds a CRLF-converted copy (3,520 B, `52d89409...`, normalises to the same value). **Prefer the LF copy; verify against the normalised hash, never a byte count.** **Applicability is NOT content correctness** — this diff contains the known-wrong P19 token swap and two contradictory comments and still passes every check. |
| GCC aarch64 cygwin cross | `fca94a35-047d-478d-8d6c-e8af786c4875` (complete) | None; result consumed | **ANSWERED — it BUILDS, it WORKS, it is LP64, it is FORK-ONLY.** Built binutils+GCC with exit 0 throughout, `-fself-test` 7,660,717 passes. 425-macro dump gives `__CYGWIN__ 1`, `__SIZEOF_LONG__ 8`, `_LP64`, 64-bit `long double`, no `_WIN32`. Upstream `gcc-mirror/gcc` master carries NO `aarch64-*-cygwin*` and ZERO `llp64` (independently verified). **Scope caveat: stage-1 bootstrap cross only** — proves data model and identity, NOT that a full toolchain builds. |
| Clang aarch64 cygwin identity | `d5162ce1-4bc1-47ff-ac9f-a4829942c8a4` (complete) | Owner decision on upstream filing | Two sealed patches, both verified by replay (v1 4/4, v2 12/12, nothing unsealed). **v1 frozen** as the audit record (data-model-neutral, `WindowsARM64TargetInfo` base); **v2 current** (`AArch64leTargetInfo`, LP64, 49 insertions). `aarch64-pc-cygwin` today emits **Microsoft-mangled C++ symbols** — a demonstrated ABI break. Upstream has no `CygwinARM64TargetInfo`; AArch64 is the ONLY arch missing a `Triple::Cygnus` case. **Not filed upstream.** |
| PR #10 CI triage | `023c70d7-43a2-4ddd-9ed6-68acfccaab93` (complete) | None; result consumed | Mechanism confirmed, **classification corrected**: not "source-non-causal / outside our control" but a one-line defect in our own workflow, fixed in still-unmerged PR #1. See the PR #10 row below. |
| Usable cross + link attempt | `1e64365a-8e29-4b6b-80ea-34408c4d868b`; `c63ab774-a023-4e57-9bc4-53f727507ada` | Owner authority for any fix | **A usable cross exists and compiles real runtime C++ source; the build REACHED THE LINKER for the first time. NOTHING LINKS, no DLL, nothing executed on ARM64, no product PASS.** Sealed port as-is = **254/310, 15 errors**; higher figures are CONDITIONAL on uncommitted diagnostics. Remaining work is finite and enumerated: `gendef` emits `sigfe.s` at 0 bytes (980 `_sigfe_*`), `autoload.cc` accounts for 192 of 196 undefined refs, 8 orphan `cygwin.din` exports, plus the version-robust SEH correction. |
| Linker/import acceptance fixtures | `473f3049-885c-495d-ad24-c4549786fe4b` | Future separately authorized ABI/linker compile-loop consumption | Terminal, sealed, independently replayed, artifact-only. Root digest `09f78c61...`; 672 inventoried files, 166 bounded receipts, and 40/40 deterministic core outputs pass. Fixture/product executions are zero; no authority is claimed. |
| Signal/TLS/fork/exec fixtures | `5e19eaff-8948-4f39-a0d7-daa13e4ee076` | Future separately authorized runtime consumption | Terminal, sealed, independently replayed, artifact-only. `SHA256SUMS` `4fcee46a...` covers 3,304 entries; 15 future tests and 126 assertions are deferred until the owning runtime layers exist. No runtime execution or authority is claimed. |
| Git Bash payload staging | `5acb4426-d1c0-40f8-91fb-2daf0806488c` | Truthful assembly harness and missing-input ledger | Terminal and frozen. 39/80 graph nodes resolved, 41 placeholders, 38 mechanics tests pass, 15 correctly deferred, zero failures. |
| Payload input materialization | Frozen materialization `b6d8b789-7781-42d7-8ffd-1d1e7e52624f`; completed review `e3ae4e76-32f6-4e38-84b6-0b0fa35e72e9`; frozen successor `c6ce98ad-52c5-441e-886a-716db53a189a` | Seven remaining node-produced outputs | Schannel transition executed once and independently replayed at root `c435e6f5...`, sums `03b1aa3d...`, execution receipt `a24562ee...`, external receipt `1ec2ef77...`. P5 is 34/34 and graph 73/80. Seven produced placeholders remain exact; no final ZIP or broader authority exists. |
| BusyBox draft PR #4 | `032439bc-767e-4cd9-8be1-4dcf140c9bf8` | Review at frozen head `942be1c` | Draft, mergeable, two successful builds. |
| Ownership/SDK draft PR #29 | `f038cc22-e1ef-4943-afd9-6f1b0b8dcdcd` | Review at frozen head `305d14d` | Draft, mergeable; one determining job passed and four non-applicable jobs skipped. |
| Runtime generator draft PR #31 | `aabca41f-e845-47dd-97cd-bc99428bc7d4` | Monitor exact frozen head `d890a845` | Draft, mergeable; 12 successful checks and six repeated external MinGW package/test failures. |
| Reformat continuity draft PR #10 | `ee27c231-950d-4128-9458-20ba8b9ce0e7` | Keep restart state current | Draft, mergeable; exact head `b0d9c343...`; seven native jobs cancelled after 24 hours queued. Cross-binutils fails at `doc/bfd.info` Error 127 (job exit 4), but triage 2026-09-03 corrected the cause: this is **not** a missing workaround. `.github/scripts/enable-ccache.sh:44` already does `ln -sf /usr/bin/true makeinfo` into `/usr/lib/ccache/bin` under `FLAVOR=CROSS`, and the log proves it ran at 01:37:13, yet configure still reported `checking for makeinfo... no` at 01:40:32. The stub never reaches the build environment, since its visibility depends on makepkg's `ccache` BUILDENV PATH injection. Suspected contributing factor, unconfirmed: line 39 sets `MSYS=winsymlinks` with no `:lnk`/`:native`/`:nativestrict`/`:sysfile` mode suffix. Not caused by this PR — all 59 changed files are under `arm64-vnext/continuity/`. **CORRECTED 2026-09-03 by A/B evidence:** of 13 workflow runs exactly ONE passed — 2026-08-13 on `crutkas-harden-arm64-bootstrap` (PR #1), the only branch carrying `dependencies: texinfo`; its log shows `installing texinfo...` then `checking for makeinfo... makeinfo` and binutils GREEN. PR #1 is OPEN and UNMERGED (verified), and `origin/main`'s binutils block has **no `dependencies:` line at all** (verified), so every branch cut from main reproduces the failure. Attribution is **(b) pre-existing on main**. The minimal fix is `dependencies: texinfo` — proven green, and the dead stub at `enable-ccache.sh:44` should be removed or repaired in the same change. **Caveat: this does NOT green the pipeline** — it unblocks 5 jobs and moves the wall to `mingw-w64-cross-mingwarm64-gcc`. No fix applied; needs commit authority. |

## Current material decisions and blockers

- Native ARM64 MSVC/SDK build-machine capability passed 18/18 final smoke
  commands. The user accepted only the documented Microsoft channel/catalog
  hash mismatch risk; other integrity checks remain required.
- Native ARM64 `windmc.exe` is reproducible across two clean builds, executes
  natively, and is hash-pinned for local engineering use. Signing is deferred
  to the Git for Windows release pipeline and does not gate runtime work.
- Runtime-ABI source work has not started. Both reported identities for the
  existing V6 directory are permanently withdrawn and non-reviewable. The
  distinct V6-R2 sibling remains atomically finalized and independently
  replayed, but review `a48221b2...` returned terminal NO-GO on four semantic
  blockers. V7 also returned terminal NO-GO and cannot be repaired in place.
  V8 is terminal and immutable. Its content-only inventory `47a630e7...`
  and verifier immutability inventory `84b7f213...` use documented distinct
  scopes; the external receipt binds both. Independent review closed all four
  V7 remediation areas but found one new blocking direct-forwarding bypass:
  `-Wp,-U__CYGWIN__`, `-Wp,-include,<absolute file>`,
  `-Wa,-march=x86-64`, and `-Wa,--defsym,ESCAPE=1` do not receive the exact
  policy exit-64 rejection. Review verdict is terminal NO-GO with no
  authorization receipt. V8 cannot be repaired in place. Its fresh V9
  replacement completed the direct-forwarding repair and full closure replay.
  Sealed additive reconciliation `ecac32cc...` and three byte-exact canonical
  inventories prove the pre-review mismatch was a receipt-hash transcription
  error plus an explicit schema difference (`mtime_ns` versus
  `mtime_ns+readonly`), not packet drift. Replacement independent review
  `522e65c4...` returned terminal `NO_GO_RUNTIME_ABI_SESSION_START`; verdict
  `bcdd8d3f...` proves one contract violation in comma-bearing sanctioned
  `--out-implib` values, including a positive 27-byte unauthorized-output
  proof. Split `-include`/`-imacros` are recorded as non-blocking observations,
  not V9 contract violations. The comprehensive contract matrix remains
  artifact-only and active; no session-start, V10, or product authority exists.
- Correction history: an earlier planning handoff described `v6-r2` as already
  atomically finalized. That claim was premature and false; it is superseded by
  the later, distinct, receipt-bound finalization recorded above.
- The payload harness is ready but cannot produce a ZIP until the runtime,
  Bash, recursive DLL closure, and other node outputs exist. Payload input
  external-input materialization is now 34/34 after the exact Schannel
  local-engineering successor. Graph accounting is 73/80; seven produced
  outputs remain placeholders.
- V9 governance: V9 is terminal via review of record `e0624c3f...` (review
  `522e65c4...`, verdict `bcdd8d3f...`). The later packet sealed by the original
  provider-blocked reviewer `0a1e87db...` is VOID for authority and is retained
  read-only as non-authoritative corroborating evidence only. Both returned
  NO-GO, so no outcome conflict exists, and it independently corroborated the
  comma-smuggled sanctioned `--out-implib` reaching clang/ld.lld. Its
  reclassification of split `-include`/`-imacros` as blockers is preserved as an
  open question for the owner and is deliberately not reopened; the owner's
  observation framing controls unless the owner reverses it. That divergence
  must not be relayed to the active V10 reviewer.
- Superseded reviewers are archived, never deleted. Archiving removes only the
  git worktree; sealed session evidence is verified to persist byte-for-byte.
- CI is monitoring information, not the reason local implementation is blocked.
- A 10-minute supervisor heartbeat advances terminal handoffs and nudges any
  critical worker with no material progress for 15 minutes.

## Historical sealed runtime-generator snapshot

The following section records the checkpoint state as of
`2026-09-01T01:25:00Z`. It is preserved for evidence traceability and is
superseded by the live tables above for scheduling.

- Canonical release graph revision 8 is SHA-256 `5d581d49b1ed3df931cd63048058bb8da9760f5c900698d49e6b6e05a601695a`.
- Exactly two vNext PRs are open: `crutkas/busybox-w32#4` and `crutkas/build-extra#29`; both are draft, uniquely bound to their reviewed heads, labeled only `arm64-vnext`, and have no queue, auto-merge, review request, or stack membership.
- Runtime-generator is clean at commit `8fbd9808447ee78ed485deead9b79cd1e40c07b7`, tree `fe1106187ef9aa842e1cff0ccc4f978b65c16613`, on `crutkas-arm64-vnext/msys2-runtime/generator`.
- Runtime-generator external requirements are admitted. Fresh ARM64 M4, Sprut, Shilka, Autotools outputs, and `devices.cc` replay are node outputs, not pre-existing external inputs. Fresh BusyBox is the exact release-graph dependency.
- Independent session-start review v2 correctly returned NO-GO on a revision-7 hash transcription and issuer-incompatible live fields. Both coordinator artifacts are repaired and a fresh replacement review is active; the rejected review remains preserved.
- Runtime-generator session-start verdict `f13ec4e688f5f9c5e9c4de38c4fdb1174b6795c81acc00e2edd31d81c8bdf9f1` and strict authorization receipt `48ade7bd7ed52ec93692df5744875607a32399e310fb778cefeeea76923d8e30` permit clean investigation, implementation, and local candidate-output production only. Hard stop before commit.
- Runtime source candidate currently changes exactly `winsup/autogen.sh`, `winsup/tests/autogen-contract.sh`, and `.github/workflows/build.yaml`. Hosted MSYS and exact native BusyBox contract tests pass; `git diff --cached --check` passes; staged tree is `43aec2ed8555b6f4a9866ae4b8605972062dff6d`. The provisional `092bb2b` tree is superseded after correcting an index-only workflow executable-mode error; `.github/workflows/build.yaml` is restored to mode `100644`. Fresh ARM64 generator-bundle build and reproducibility evidence remain in progress.
- Independent source preaudit `cb76badf249ea5b2844b5fab608072edb90cc00f3a949959c0cba112e0bbd71b` passes with zero blockers and grants no commit authority. Final review still requires the generator bundle, 10-output replay, provenance/process/PE/import/module records, and two-build byte reproducibility evidence.
- Emergency runtime continuity is frozen as exact patch `0f2f3f9dfc7509d1d240f81a44a4c1700032478c51ff89d769a14c4b5ca022d8`, sealed state ledger `6ff2bcc368087070a9070729e2ea888b6f466eafbffb8c083db8f2a23cd58b69`, and local replay archive `e5113678ade3f4e79a63b473eafe55e164839a06dba20d8f8cf3a63996e6ffc1`. Patch and ledger are remote in continuity draft PR #10; the 135 MB archive remains a reproducible local fallback.
- Continuity draft PR `crutkas/msys2-woarm64-build#10` is remote at commit `d27a45a135d28042f8b34c70e91f82fec4611903`, tree `6e102e71fe93fc02a1ad9bbca877299e4c16354a`. It contains 23 files/2,606,505 bytes, exact byte-preserving state, `SHA256SUMS` SHA `30b15351162284da52a61fa573a11fdffae92f638e2e8f9ae509f7493b86f77d`, 14/14 seal replay, secret-scan pass, and exact runtime patch-to-tree replay. Cloud handoff session `dae16354-b5f6-4db5-9026-916a0fb3b03c` is verifying it read-only.
- Runtime before-commit review `0e88bb5e997b2a437faada948376fb9559a23ecb653567dc3111efd1718057d7` returned NO-GO on one closure defect: native `tools/make.exe` imports admitted `libintl-8.dll` SHA `31db0d0e7780cf28dca1309a894cb775b2ab130c4ba777c546a206970ca47320`, but the DLL was absent from the bundle/manifest/ZIP. An isolated bundle PATH reproduces `STATUS_DLL_NOT_FOUND`; ambient CLANGARM64 PATH had masked it. The writer is rebuilding candidate outputs and evidence with recursive isolated import closure. Staged source tree `43aec2ed...` and zero-commit hard stop remain unchanged.
- Revision-2 repair bundles that exact admitted DLL. Two independently written ZIPs are byte-identical at SHA `bf5e5cfae801f5e6c5a79acbf25e99b7a26ea9bed3c67895ceed979a28ca49e1`; 119/119 PEs are AA64 and 1,348 recursive import edges have zero unresolved entries. The missing-DLL control still reproduces `0xC0000135`, while repaired `make.exe` and all required generator tools pass under bundle-only PATH plus System32. Strict diagnostic `2f2ec26efe0b7b59e6c60db7c0d363a0caff2b6a57ae660a29c12d79f7e05c14` passes; fresh independent re-review is active.
- Replacement review `c286ab7afc584c0102797afe5a94ccf8a4598f65897e191121edb74afbe67dff` confirmed every runtime source/artifact gate but returned NO-GO because it resolved the canonical controls path to preserved earlier Phase-0 controls. The old `d11d2a1...` file is archived unchanged; exact revision-8 controls `95d907f4...` are now canonical under sealed promotion `35cb98a81feeb0626c9ee6ed35fbd7c62224da8f2d09ba0a6fc02b93851e08ec`. Runtime artifacts did not change; a narrow carry-forward review is active.
- Carry-forward review `bd7d6f358583833b76388e9e453ca24735804029a493b0bc1ec3a6ec3c6084e6` is zero-blocker GO for exactly `runtime-generator:[before_commit]`. Issuer verdict `ee4bdd0592b689bedece731d384c59854657f459c8bc7e2cfa527ce89922ce22` and strict receipt `00711096c30d6e3aa6c0d5ef05c41ae0af252e604adb4924842d13e376801cc7` permit one commit of tree `43aec2ed...`; push and PR remain forbidden.
- Authorized commit `d890a845e992638a6f09560efacc26d15b3ffe6a` was created exactly once with tree `43aec2ed8555b6f4a9866ae4b8605972062dff6d`, parent `8fbd9808447ee78ed485deead9b79cd1e40c07b7`, required trailers once, and clean worktree. Remote branch and matching PR counts remain zero. Graph revision 9 SHA `9a3b3fd88582b0201efb1b5dd943936976d8c4224ed7b2c608c041be6cb34b72` is staged with passing controls; independent HEAD audit is active.
- Independent HEAD audit `05a1d065ae87b6b4160af3802fb5f3745e64d5ebea3b15cb61f7001b166035d8` is `GO_FOR_PUSH_REVIEW` with zero blockers. Canonical graph revision 10 `e18c31f5958ea66a52472a13ac126c064f86b51e461ac1eafb0521b5223b6003` records `audited_waiting_push`; controls and strict push diagnostic pass. Remote branch/PR remain absent while separate before-push review runs.
- Before-push review `b0b3c78c3afc68fb492b517da79497cbedcd595e737fcf08fe67c501fea6d593`, issuer verdict `b3b76eeeaf5a31722e110c31db6a13708a00753e27937ad9f94093d67c53a45f`, and strict receipt `567922eea1dff103f65ddda2635c6df7cdf92f7b37668f8545ce92cac4fae8b8` authorize one normal non-force creation push at exact head `d890a845...`; no PR authority.
- One normal non-force push created only `refs/heads/crutkas-arm64-vnext/msys2-runtime/generator`; remote commit/tree/parent read back exact, local upstream matches, worktree is clean, and PR count remains zero. Push receipt SHA is `f89e56d88a333322cf0fef278698aa6fc13751f00def7c7941c1cf799ad8f3a1`. Canonical graph revision 11 records `pushed_awaiting_pr`; exact title/body and independent before-PR review are active.
- Before-PR review `86a27db3e67abb301adb4760bb5eefd37fafd24bf890aec46cd341784791f7c7`, issuer verdict `61c26a2e44f9ad09b7c2fa2d4a4d058e3b049a78619d1a50cd89bd315cc3a6ce`, and strict receipt `69f33b882236e895758623e4cdca8cd22020e0eaacfa3cbae3e5d33521feeeba` authorize exactly one draft PR with body SHA `f29ca2873dd21c9ce23877daf70454cfec201ce8c6781652d92b5548a1932f9d` and only `arm64-vnext`.
- Runtime draft PR `crutkas/msys2-runtime#31` is open at commit `d890a845e992638a6f09560efacc26d15b3ffe6a`, tree `43aec2ed8555b6f4a9866ae4b8605972062dff6d`, base `msys2-3.6.10`, exact reviewed body, label only `arm64-vnext`, unique head count 1, no reviews/queue/auto-merge/stack/merge. PR receipt SHA is `ee7b28eb7db94e1b848359d3f49bfc85bc3856610755ee5303e8b4cee8e36262`; graph revision 12 and four-PR live readback are sealed.

## Coordinator work

| Item | State | Next action |
|---|---|---|
| Independently replay `arm64-old-world-quarantine.json` | Active | Verify aggregate seal, per-repository receipts, and ambiguous-PR result |
| Maintain task graph, inbox, and plan | Active | Update on every report, blocker, decision, or status transition |
| Reconcile superseded kickoff digests in sealed receipts | Active | Preserve old receipts and issue a new epoch mapping to the final policy |
| Complete omitted build-extra forbidden inventory | Done | Two supplemental files independently rehashed and replayed |
| Complete omitted runtime forbidden inventory | Done | Corrected epoch-named fragment and 823-artifact ledger verified |
| Resolve BusyBox dependency | Done | Fresh bootstrap-tools layer is required now; permanent dependency deferred |
| Collect normalized owner fragments | Done | All eight fragments and sealed index independently verified |
| Generate boundary, graph, ancestry, POC ledger, and artifact contract | Reopened | v1 preserved; v2 fixes head overbreadth, utility edge, and staged identities |
| Implement mechanical boundary verifier | Reopened | v1 rejected as fail-open; v2 requires source equality and authoritative readback |
| Verify real first-layer session starts | Rejected | v1 receipts omitted invocation and complete object-coverage evidence |
| Maintain authoritative status | Active | Keep `arm64-vnext-status.md`, this inbox, and `plan.md` synchronized |

## Triaged

### `crutkas/busybox-w32`

- Report received from session `b6e4a22b-4030-4c25-9f1c-63985633cbeb`.
- Repository Phase 0 verdict: **GO**.
- Clean base: commit `d8d8bb397f1e200ba5a871dc6aa4af819b23f32a`, tree `37fb55f5335e63c9f14c44208ed08a6e9aad4f91`.
- Quarantine and receipt seals matched; ambiguous open ARM64 PR count is zero.
- A fresh `busybox-w32/bootstrap-tools` layer is **required for the current first artifact** because it supplies the native bootstrap shell/core applets and current command payload.
- BusyBox is not a permanent product dependency; replacement with native runtime/package tools is post-handoff backlog.
- Old BusyBox commits, branches, workflow artifacts, and binary hashes remain forbidden inputs and reference-only evidence.

### `crutkas/msys2-woarm64-build`

- Report and correction received from session `02dddb73-7cc1-42bd-84b0-5c91974bb175`.
- Repository Phase 0 verdict: **GO**.
- Clean base: commit `77f0d56cdf4c063c1b3af37f551e01c3a012c8bb`, tree `a031061a532b57adf447fcb7df478d9b37a071a6`.
- Quarantine replay passed; ambiguous open ARM64 PR count is zero.
- Old native stack `8`, the stale Gettext edge, all non-main refs, 16 old/candidate heads, synthetic merge commits, workflows, and 39 Actions artifacts are forbidden. Exactly five artifacts remain unexpired; expiration does not change the deny.
- Proposed new stack: `bootstrap-foundation` -> `gettext-source-build` -> `protected-boundary`.
- Implementation remains blocked on exact admitted GCC/binutils identities, package-recipe identities, official Gettext/libiconv sources and digests, and downstream runtime/packaging identities.

### `crutkas/binutils-woarm64`

- Report received from session `b2500699-4946-421b-a30e-1208d18239d5`.
- Repository technical verdict: **GO after Phase 0 evidence repair**.
- Clean base: commit `44335833f8f734f978211b082b15aed14efcf958`, tree `1164c74fe058fd8a561ee16670e674562649b5f8`.
- Live quarantine, zero-ambiguity query, and all old-head ancestry exclusions pass.
- The exact source applied receipt and its payload seal pass. The aggregate embedded copy changed three timestamp spellings and does not replay its declared payload seal.
- Proposed new stack: `source` -> `hardening-tests`. Build evidence is an epoch artifact by default, not a third source PR.
- The exact cross-repository consumer edges remain to be established by the aggregate release graph.

### `crutkas/mingw-w64`

- Report and clean-base follow-up received from session `4df7471a-2de2-4f43-a000-bfb897e17ce2`.
- Visible quarantine passes with the sealed `viewerCanLabel=false` exception for upstream PR #184.
- Canonical upstream base `6df76fa527c36e770217ddd763adaaf37bd2887f`, tree `c1a129a1537210b8decf2f8292338526684435af`, is now independently present and verified in isolated storage with no final live drift.
- Both old heads are excluded from canonical-base ancestry.
- The applied receipt seals against a superseded kickoff digest and must be reconciled by new vNext evidence.
- Current minimum-MVP proposal is zero mingw source delta. Workflow-pin maintenance is separate and not on the first-artifact critical path.

### `crutkas/MSYS2-packages`

- Report and corrected PR #22 identity received from session `6105ed30-3b5e-4e9a-8ad1-5f2e94f72893`.
- Clean base: commit `73248abe6bc25e73486c29f876094b3eeab79547`, tree `fed85f3aa739acc838617b2561c81147a70ac9ad`.
- Quarantine and all ancestry controls pass.
- Exact deny inventory includes 45 PRs, three old stacks, 21 releases, 106 release assets, and 451 relevant live workflow artifacts.
- Proposed MVP stack: `berkeley-db-c23` -> `bounded-source-locators`. Admission policy is separate and conditional.
- Base-controlled admission remains unavailable because `master` is unprotected and no independent code owner exists.
- Minimum-DAG review found no exact first-artifact dependency. The entire package stack is deferred to self-hosting, package-native builds, or RTM.

### `crutkas/gcc-woarm64`

- Report received from session `2877160a-3fdb-4cad-90d6-c173b8d04eb0`.
- Clean base: commit `5688a17320e775944bbe795010ebe7e89fc7a628`, tree `fbab8c5cc25083a857a15bb1240879497da66b02`.
- Quarantine and all old-head ancestry exclusions pass; old stacks `8` and `14` remain forbidden.
- GCC contributes zero layers only if an exact official MSYS-target compiler is admitted. Otherwise the minimum is `runtime-foundations` -> `c-bootstrap-preflight`.
- Extended SEH, C++, libffi, LTO/plugins, and generic plugin argv handling are post-MVP or explicitly blocked.
- The first path now selects admitted LLVM/LLD and defers GCC; the two-layer GCC path remains the explicit fallback.

### `crutkas/msys2-runtime`

- Report, corrected fragment, and exact 823-artifact ledger received from session `2c5e9beb-2617-4e37-a8e0-b835010b6205`.
- Clean base: commit `8fbd9808447ee78ed485deead9b79cd1e40c07b7`, tree `fe1106187ef9aa842e1cff0ccc4f978b65c16613`.
- Quarantine and ancestry pass. Closed PR #20, expanded refs/heads, two releases, and all 823 artifact IDs require new sealed evidence.
- Proposed MVP stack: `generator` -> `abi` -> `linker-import` -> `signal-tls` -> `mvp` -> `fork-exec`.
- Utilities and CPU topology are deferred. LLVM/LLD is the selected first path; binutils/GCC become fallbacks if required behavior fails.
- Initial non-epoch/insertion-order evidence remains rejected and unchanged. Corrected epoch-named files independently replay.

### `crutkas/build-extra`

- Report and sealed supplemental evidence received from session `0f21bfbc-b7ec-472c-98fa-cf80e4ccc1ad`.
- Clean base passes at commit `7acc5756b94a0a0ab8d0cd06642c8c8254e1bf40`, tree `08e34404480e5fd67cfdbce904a09e5a35c49871`.
- The aggregate omitted closed/unmerged old ARM64 PR #2, 1,612 Actions artifacts, two ARM64 releases, and their assets. Embedded source seals also fail after timestamp reserialization.
- Proposed stack: `ownership-sdk` -> `payload` -> `aggregate-check` -> `governance`.
- Exact first artifact: `arm64-vnext-2026-08-31-v1-git-bash-mvp-arm64.zip`.
- `main` currently has no protection or ruleset; protection must follow merged base-controlled checks and observation on a fresh harmless PR.
- Supplemental boundary file SHA-256 `a253025ce6f3dd8c1e51834c5a164526b95e42fc0f13695a17b7bd151a60f676`; 1,612-artifact ledger SHA-256 `b0fb454762368f5d6e27e8b287ee1c217eb0a203b4d27c3b404d53eb1c3ef7f6`. Both payload seals replay independently.
- Minimum-DAG review permits `ownership-sdk` -> `payload` -> artifact -> independent replay as a loudly **NON-ADMITTED ENGINEERING HANDOFF**. Aggregate checks, governance, protection, and a fresh superseding artifact follow before any authority claim.

## Historical Phase 0 decisions and blockers

- Product implementation remains **NO-GO** until the aggregate Phase 0 boundary, release graph, ancestry report, verifier, artifact contract, and independent review pass.
- Independent review SHA-256 `eb3777a9a2b69324439ed1876db6d75038e632e3e532a6cfc3a86c558e113ff1` found four blockers: fail-open verifier, incomplete ancestry coverage, contradictory utility ownership, and unfrozen source/input identities.
- The v1 `300`-head boundary was overbroad. The corrected model retains all `348` denied branch-ref records but only `173` actual POC/candidate commit heads.
- Payload ledger v2 SHA-256 `857984d61f37862eb0ada5c92bb21699e301c5a861c65c8abe3829978cb33f7b` selects 31 files with 15 ARM64 PEs, 16 data files, and zero x64/x86/ARM64EC/unknown PE.
- Public HTTPS uses Schannel with GCM disabled; trust uses the selected Windows store; SSH uses inbox ARM64 OpenSSH by reference with artifact-owned bounded configuration.
- Final consolidated provenance SHA-256 `ad691d7d5683d5caee6ffb7abbb5c430eabb492cfc5d52ca01bbb5453579972a`: 184 records, 174 admitted, five blocked exclusions, five reference-only, and every required node group admitted.
- Final staged v2 boundary, graph, ancestry, artifact definition, 14 controls, and strict BusyBox/build-extra diagnostics all pass; independent re-review is active.
- `vnext-resolve-binutils-receipt-seal` is active. Old evidence stays unchanged; the resolution is a new sealed vNext replay referencing the exact source receipt and live state.
- BusyBox dependency reconciliation is complete: one fresh layer is in the minimum graph; old BusyBox commits and archives remain forbidden.
- Package and bootstrap critical-path decisions are complete: both repositories have zero first-artifact nodes.
- LLVM/LLD is the first runtime linker/compiler path. GCC and binutils remain explicit fallback paths only.
- `vnext-collect-boundary-fragments` and `vnext-reconcile-policy-digests` are active.
- Verified owner fragments currently exist for binutils, runtime, GCC, mingw-w64, MSYS2-packages, bootstrap, and build-extra.
- Coordinator quarantine replay SHA-256: `f68df17b1d7945f667885291240773207b0f53481e061f504131423953e1f020`; it proves all 16 referenced source files hash correctly and records 44 lexical differences across seven embedded copies.
- Boundary/release/verifier v1 hashes remain preserved as rejected or superseded evidence; do not use them for writer authorization.
- Final boundary SHA-256 `db1fe7fb9dcc133e637d9fb7d53ec4c37236c026c0941cddb58d274ff55e4439`.
- Final graph SHA-256 `101e58d029b1e3d21269adbf68624fd6e8fdaf74b57c2dd90f6fdc7430478509`.
- Independent review SHA-256 `583709d37676f7a9e3163dd38403f939224e79c138d6e453b51051a61d333281`; corroborating review `e524bfc563c8cf3d71c71fb0936243920acda078ce4609a8fd7c80c95175c57e`.
- Phase 0 verdict SHA-256 `8dede4ade80cfa9b17366a7ec521803d2e79d5adc5dd68d382c0269a5c5aab25`.
- Only BusyBox and build-extra ownership may edit and commit. Neither may push or open a PR until a new graph and authorization are sealed.
- That authorization is temporarily invalidated. Both sessions proved exact clean bases, then stopped because app-native rename enforced `crutkas-`; no source edits or commits exist.
- Amendment SHA-256 `e477de0e24d84c40843a887a0b5b2268257e6373faa1c22ea6490227fdc93a8c` is independently reviewed.
- Amendment review SHA-256 `61ef7605fa913895566c9cd60cf702148dcd4128340e5e1a7f495c46a91c3848` passes with no blockers.
- Replacement verdict SHA-256 `67b8b06d4a051998c2d64a7b65c526378c751c53f6e049743c98e8195b4adcc9`.
- Actual branch authorizations: BusyBox `e7107b1d41f59a056a68dab1f9161daf93babfa9280f23bce8b4f595664dea34`; build-extra `dbb8ea5294015f03a705c5440aa179f000c871aecb64550f06ec39830785ac33`.
- No old-world object may be mutated or consumed as a vNext build input.
- Any newly discovered task must receive a `vnext-*` ID before work begins.
- clangarm64 availability claim re-derived: five build tools genuinely absent; `sh` claim false as stated but correct in consequence.
- Native ARM64 shell `brush` 0.4.0-2 exists and executes; blocked for autoconf solely by the missing `exec` builtin.
- `make` is native but named `mingw32-make.exe`; scripts invoking bare `make` will fail unless aliased.
- Both perl modules gating `2918d1f1`'s native-vs-cross decision are present in core perl.
- Sealed port is NOT link-capable for aarch64: cygwin.sc.in hard-#errors on non-x86_64. Every ARM64 link used a modified copy that exists in no archive.
- All four x86-gated conditionals are original defects present in the seal, not regressions.
- .xdata resolved by own measurement on 4 ARM64 images: works by orphan placement, not by rule; fix the asymmetry anyway.
- FLAG: msys-2.0-ctorfix.dll carries DllCharacteristics 0x0160 (ASLR on) while msys-2.0-fixedbase.dll carries 0x0100; build order NOT established.
- ASLR: DYNAMIC_BASE is MANDATORY on Windows-on-ARM (6/6 stock DLLs, err 193 when cleared); HIGH_ENTROPY_VA is NOT. --disable-dynamicbase is a non-fix. ASLR-regression flag CLOSED.
- Fifth pattern class recorded: architecture assumptions encoded as numeric literals are undetectable by #ifdef search and need a separate sweep.
- path.cc find_fast_cwd_pointer() is an x86-64 machine-code scanner but IS ARM64-guarded at its call site - true negative, corrected before reporting.
- Native ARM64 git.exe VERIFIED on the five-point protocol plus toolchain attribution and known-SHA clone; rev-parse HEAD matched 7fd1a60b exactly, fsck clean.
- Windows TLS demonstrably NOT involved: schannel/ncrypt absent from the live process tree, with Windows curl.exe as a firing positive control.
- CAVEAT: driver-bin/mingw32-make.exe is LLD-linked clangarm64 package make, not WoA GCC. Artefacts are single-toolchain; the driver is not.
- My own first TLS sweep returned zero after one sample and was DISCARDED as absent-not-negative. The detector rule caught its author.
- cygheap x86_64 differential run independently: chain HEALTHY (33 entries, terminates, all in-heap), lowest entry exactly cygheap+0x48A0 = sizeof(init_cygheap).
- Geometry constants sizeof=0x48A0 and offsetof(chain)=8 independently confirmed on a different arch/version/build.
- Corruption is NOT an upstream defect; ARM64 attribution supported. Limitation: upstream 3.6.9 is not our build, so strong-not-definitive.
- MY OWN BUG: containment test treated the entry b field as a size; it is a BUCKET INDEX (size = 1<<b). Uncorrected it produced a false disconfirmation of the ARM64 evidence.
- INSTALLED TREE VERIFIED. bin/git.exe = 69b1e704729cf69f0a0c029aa189eb9d38f74fc16ef37823048cb6a98b1523d1, re-read unchanged at report time.
- 158 .exe, all 0xaa64, ZERO hardlinks and ZERO symlinks - NO_INSTALL_HARDLINKS=1 worked; pacman -Ql will enumerate all 158.
- TREE IS NOT FROZEN (0 read-only, no manifest). Consumers must pin the hash and self-refuse.
- MY OWN BUG: BY_HANDLE_FILE_INFORMATION needs Pack=4; default packing produced impossible link counts and a false HARDLINKS PRESENT that would have blocked the release.
- malloc.cc:1670/1676 win32mmap/win32direct_mmap citation CONFIRMED exact against sealed source.
- MEASURED: neither call produces 512-887 GiB here (0.7-1.45 GiB bottom-up; ~131,019 GiB top-down).
- Cygwin layout has NOTHING at 512 GiB; USERHEAP_START is 40 GiB, MMAP_STORAGE_LOW is 64 GiB.
- The distinctive .003 appears exactly once: CYGHEAP_STORAGE_INITIAL 0x800300000 = 32.003 GiB = 512.003/16, and matches the live cygheap commit limit I measured.
- MEASURED: DLL ASLR is PER-IMAGE-PER-BOOT. ASLR-enabled DLL (0x4160) loaded at one identical relocated base across 5 concurrent processes and 3 sequential reloads.
- My first version of that test used an ASLR-DISABLED DLL (0x0000) and had no sensitivity; discarded before use.
- Session 68c032ba independently re-derived --disable-dynamicbase as the fork fix. WARNED: premise false, change harmful (err 193).
- RETRACTED: the cygheap wild-value redirect. Raw bytes = 0x80002CA000 = 512.0027 GiB at full width; my one-hex-digit hypothesis falsified.
- MMAP_STORAGE (64 GiB..114,688 GiB) CONTAINS 512 GiB; my 'nothing in the layout is at 512 GiB' was false - I read ranges as point values.
- The .003 was a rounding artefact, not a fingerprint. Never derive an identifier from a rounded rendering.
- Withdrawn: the claim that my errors are always in the instrument rather than the reading. This one was reasoning.
- LESSON: A CONDITIONAL IS ONLY SAFE IF ITS RESOLUTION TRAVELS AS FAST AS THE CONDITION DID. My hedge propagated correctly to 68c032ba, then outlived its own test by one message; they recorded it as live-conditional after the condition had already failed.
- An UNRESOLVED condition sitting in a document is indistinguishable from live work - the mirror image of a conditional decaying into a fact. Both are failures of the same channel.
- CORRECTED: cygheap entry sizes are bucket_val[b] (explicit table, powers AND medians), NOT 1<<b. bucket_val[15]=6144, not 32768.
- My 'needle owned by 0x8000050B0' was a FALSE POSITIVE from the inflated extent. True owner is 0x8000068C0 (b=0, 32 bytes).
- STRONGER RESULT: x86_64 and ARM64 allocation sequences are BYTE-IDENTICAL through 0x8000068F0; divergence is exactly one field.
- EXPECTED VALUE MEASURED: prev of 0x8000068F0 is 0x8000068C0 on x86_64 - that is what ARM64 slot 0x8000068F8 should hold.
- I now OWN the ASLR/dynamicbase question programme-wide; re-proposals route to me and may be refused flatly with the err 193 table.
- MEASURED: main-thread StackBase (TEB+8) = 0x800000000 = cygheap base, on 2 MSYS2 programs. THREAD_STORAGE_HIGH and CYGHEAP_STORAGE_LOW are the SAME address, so the regions abut and StackBase+8 aliases cygheap->chain exactly.
- CONSTRAINT: that precondition is ARCHITECTURE-NEUTRAL (measured on x86_64 where fork works), so the hypothesis needs an ARM64-specific CODE difference, not a layout difference.
- ATTRIBUTION: the __getreent/StackBase hypothesis was attributed to me by 68c032ba but did NOT originate in this thread; it came from the 290c9aaf supervisor context. Standing caveat retained: I cannot prove my identity did not produce it.
- MECHANISM CLOSED TO ONE OPERATION: sealed config.h __getreent reads %gs:8 (= TEB StackBase) and SUBTRACTS __CYGTLS_PADSIZE__ 12800; cygtls.h:304 agrees for _my_tls.
- Arithmetic corroborated: 0x800000000 - 12800 = 0x7FFFFCE00, the exact value my FIRST heap dump recorded at entry 0x8000050b0 data+0x10.
- Sealed port has NO ARM64 __getreent (#error unimplemented) - so the ARM64 version is NEW PORT CODE, satisfying the ARM64-specific-in-code constraint. Fifth #error instance.
- ONE-READ TEST: on ARM64 print __getreent()/_my_tls address. 0x7FFFFCE00 = correct; 0x800000000 = defective and a +8 store hits cygheap->chain.
- CAUTION: reading the TEB via x18 correctly and doing the -12800 correctly are TWO separate fixes; only the first is confirmed done.
- MEASURED: ARM64 __getreent exists in ZERO reachable archives; all 63 config.h copies are one version carrying only #error unimplemented for this target.
- SECOND INSTANCE: no modified cygwin.sc.in with __aarch64__ in any archive (0 of 60). The ARM64 port source is SINGLE-COPY on /root/xc/runtime, unarchived and uncommitted.
- Escalated as higher priority than remaining technical items: losing that host loses the port.
- The one-read __getreent discriminator returned NEGATIVE; __getreent and signal trampolines eliminated by measurement. OS loader during lazy DLL load is the sole surviving candidate.
- HYPOTHESIS (antecedent UNTESTED): if sizeof(_cygtls) > __CYGTLS_PADSIZE__ (12800) on ARM64, _my_tls writes overflow into the cygheap; offset 12808 lands exactly on cygheap->chain.
- MEASURED basis: _my_tls occupies [StackBase-12800, StackBase) and its upper bound IS the cygheap base; _cygtls EMBEDS ucontext_t at cygtls.h:193; __mcontext is #error for non-x86_64 at signal.h:102, so the ARM64 port AUTHORED the definition that determines sizeof(_cygtls).
- CRITICAL: there is NO static_assert anywhere that sizeof(_cygtls) <= __CYGTLS_PADSIZE__, though the tree uses static_assert in 6+ other places. Nothing at build time would catch an overflow.
- I PUSHED BACK ON MY OWN HYPOTHESIS being promoted to airtight: the GEOMETRY is airtight, the HYPOTHESIS is not. 'It explains the ASLR-varying value' is CIRCULAR - that only follows if the hypothesis holds.
- Seventh x86-gated #error instance found: cygtls.h:243-251 _cygtls::lock() uses __asm__(pause) with #error for non-x86_64.
- Flagged: signal.h:107 __COPY_CONTEXT_SIZE 816, an x86-derived hard constant commented as not sizeof(CONTEXT) due to historical accidents.
- TEST, unchanged: print sizeof(_cygtls) on ARM64 and compare with 12800. Everything else is scaffolding until that number exists.
- SIXTH MISATTRIBUTION: 68c032ba credited me with a 16-row elimination table and a claim about cygtls.h:313-329. I produced neither. Recurred IMMEDIATELY after the fifth was corrected, and this time an ERROR is attached to my name.
- SCOPING PRESERVED: my finding was "no static_assert on sizeof(_cygtls) <= __CYGTLS_PADSIZE__ IN THE SEALED PORT". Verified again: sealed cygtls.h has ZERO static_assert of any kind, and NO file in the sealed tree contains both static_assert and CYGTLS_PADSIZE. c63ab774 added one TODAY in its working tree - both statements true, no contradiction.
- EIGHTH x86-only construct at sealed cygtls.h:326, in the class san constructor: inline asm moving rsp into the frame field. x86_64 asm with NO ifdef and NO error guard - the FIRST in this family with no guard at all. Sits in the myfault exception path; the port must have rewritten it. Recorded as SURFACE AREA, not as a candidate - supervisor has stopped hypothesis generation.

- ** RESOLVED - FORK ROOT CAUSE FOUND AND PROVEN (supervisor 290c9aaf, 2026-09-03 ~14:30-16:10).** Supersedes "OS loader during lazy DLL load is the sole surviving candidate" and closes the sizeof(_cygtls) line: BOTH were wrong. Caught live on hardware with a page-protection debugger (hardware watchpoints do NOT fire on WoA; PAGE_READONLY + EXCEPTION_ACCESS_VIOLATION does).
- FORK MECHANISM: dcrt0.cc:1054-1060, ARM64 arm of the main-thread stack switch. create_new_main_thread_stack (create_posix_thread.cc:276) returns StackBase-16. The x86_64 arm then does subq \,%rsp; the ARM64 arm subtracts NOTHING, with a comment asserting "Windows ARM64 has no shadow space, so ... nothing has to be subtracted here". That reasoning is wrong: x86_64 addresses locals at NEGATIVE offsets from rbp (below StackBase), AArch64 at POSITIVE offsets from sp. The compiler emits str x4,[sp,#24] at 0x180046cd8 -> StackBase+8 == cygheap->chain.
- FORK EVIDENCE: faulting Pc captured 3/3 identical runs; chain walk WILD at depth 39, victim entry 0x8000068f0 (matches the previously recorded 0x8000068F8 exactly); wild value == TEB confirmed 3/3 tracking ASLR.
- FORK FIX (measured, not guessed): frame analysis of _dll_crt0 shows max reach above sp = 32 bytes vs 16 available -> shortfall exactly 16. Binary patch sub sp,x0,#16 -> fork PASS 3/3; #256 -> PASS 3/3. RECOMMEND subtracting 32 to match the x86_64 arm (16 leaves ZERO margin; frame size is compiler-determined and can grow silently), PLUS a guard page between THREAD_STORAGE_HIGH and CYGHEAP_STORAGE_LOW so any future overrun faults loudly.
- ** RESOLVED - SECOND CRITICAL DEFECT: COMMAND-LINE ARGUMENT CORRUPTION (previously unknown).** dcrt0.cc:165 and :167 in quoted() call strcpy(cmd, cmd+1) and strcpy(p, p+1) - OVERLAPPING source and destination, which is undefined behaviour. Upstream Cygwin latent bug: x86_64 byte-forward strcpy tolerates it; ARM64 strcpy aligns the source DOWN to 16 bytes and loads NEON 16-byte blocks (and x2,x1,#0xff..f0 / ld1 {v0.16b},[x2] at 0x1801ec4c4), re-reading bytes its own stores overwrote.
- ARGV EVIDENCE: exact byte-for-byte reproduction outside the runtime - input "...cmdprobe.exe" abcdefghijklmnop yields strcpy='acdeefghijklmnop' vs memmove='abcdefghijklmnop', identical to the corrupted argv[1] measured live. A/B with return to baseline (corrupt/clean/corrupt, same directory, only the DLL swapped). Tree-wide sweep finds EXACTLY these two sites; no other overlapping copy exists.
- ARGV FIX: memmove(dst, src, strlen(src)+1) at BOTH call sites. Fix belongs at the call sites - strcpy is behaving correctly, the callers invoke UB.
- ** P3 IS 7/7 PASS with both fixes applied** (baseline 5/7): malloc file setjmp sigsetjmp tls signal fork. sigsetjmp had appeared to fail only because the argv bug corrupted the test NAME to "sigsetjmpp" -> "unknown test"; with argv fixed it runs and passes, independently confirming the argv diagnosis.
- INSTRUMENT NOTE for future work: hardware watchpoints arm and persist on WoA (Wvr/Wcr retained across 24 debug events on two different addresses) but NEVER fire. Page protection is the working substitute. Also: DEBUG_EVENT union is at offset 16 not 12 on 64-bit; Windows ARM64 needs brk #0xF000 (brk #0 is delivered as ILLEGAL_INSTRUCTION) and reports Pc PAST the brk; PowerShell parses 0x80000003 as a negative Int32, silently failing every exception-code comparison.
- SWEEP FOR SIBLINGS - CLEAN: no second overlapping str*/mem* in the tree. slashify/backslashify pass the same buffer twice but are byte-lockstep loops (safe). mount.cc:889,910 copy between distinct buffers (all callers checked). math/pow.def.h x87 asm is properly guarded via #else fallback. All 18 sampled long-double symbols are exported and math/aarch64/longdouble.o exists - present and linking, though NUMERICAL correctness on ARM64 remains untested.
- OPEN, NOT CLAIMED AS A DEFECT: argv[0] loses its C:/Users/... prefix identically on patched and unpatched builds. Probes run in a bare directory with no MSYS2 installation or mount table, so a degenerate cygwin_conv_path is the likely explanation. Worth one check in a real installation.
- ** BUILD IS NOT REPRODUCIBLE - five confirmed defects (supervisor 290c9aaf, 2026-09-03 ~16:40-17:40).** Found by ATTEMPTING the rebuild in a cp -a copy, not by inspection. The link session's tree was never modified and was verified byte-identical throughout.
- DEFECT 1: Makefile.am "if TARGET_AARCH64" lists 11 aarch64/*.S files that DO NOT EXIST (x86_64: 11 referenced, 11 present). make dies immediately: "No rule to make target 'aarch64/bcopy.S'". Workaround verified: empty am__objects_1/am__objects_13; safe because the generic routines measured clean (21,840 tests, 0 failures) and the resulting libdll.a matches the original at 250 members.
- DEFECT 2: scripts/gentls_offsets greps for '\.long' at lines 65 and 88, but ARM64 GCC emits '.word'. Regenerating tlsoffsets yields 56 bytes of ZEROS instead of 1,822 B / 59 entries, after which sigfe.s cannot assemble. LIVE HAZARD: running make in the real tree silently destroys a working tlsoffsets.
- DEFECT 3: the windres invocation lacks include paths - winver.rc:2:10: fatal error: cygwin/version.h: No such file or directory. Fixed by passing -I<runtime>/include -I<bld>.
- DEFECT 4: the link rule (Makefile:3247) uses $(CXX) $(CXXFLAGS) and NEVER references $(LDFLAGS), and the required -L paths (build-gcc2/.../libgcc, implibs/lib) appear NOWHERE in the build system. Injecting via CXXFLAGS works.
- DEFECT 5 (MOST SERIOUS, SILENT): __MSYS__ is not predefined by the compiler (it defines __CYGWIN__), not in config.h, and not added by the Makefile. dcrt0.cc:1102 is "#ifdef __MSYS__ msys_dll_init() #else cygwin_dll_init()". The documented build command therefore produces "T cygwin_dll_init" where the shipped DLL exports "msys_dll_init" - compiles clean, no warning, WRONG EXPORTED SYMBOL. Fixed with -D__MSYS__; verified by symbol dump on both objects.
- ** RETRACTED - DEFECT 6 WAS FALSE.** I escalated that fenv_aarch64.o had "no source anywhere" and was unrecoverable. WRONG: the source is at math/aarch64/fenv_extern_aarch64.c (also fenv_aarch64.c, w-orphans/, and session 6be97d50's files), and the object's own DWARF names it. My error was running "ls .../winsup/cygwin/fenv_aarch64.*" - the PARENT directory - and generalising a one-directory check to "anywhere on the host", then escalating before searching exhaustively. It rebuilds cleanly. The real issue is only that the Makefile does not reference it.
- ** SOURCE FIX EXONERATED BY CONTROL EXPERIMENT.** A DLL built from the fixed source links but crashes 0xC0000005. CONTROL: same tree, same five workarounds, ORIGINAL dcrt0.o substituted -> crashes IDENTICALLY. Therefore the crash is caused by a build workaround, NOT by the fork/argv source changes.
- BASELINE ERROR CORRECTED MID-INVESTIGATION: I had been treating FROZEN (490dfe09, 09:42) as "the working DLL" while rebuilding against the w-link tree, whose DLL (20ad86ba, 12:24) is a DIFFERENT later build I had never tested. Tested: 12:24 gives malloc PASS, signal PASS, fork FAIL - a healthy baseline with fork unfixed as expected.
- ELIMINATION of every measurable link input between the working 12:24 build and mine: cygwin.sc IDENTICAL; version.o, uname_version.o, tlsoffsets IDENTICAL; libdll.a 250 members each differing only in the fenv object's FILENAME; sigfe.o forced to the original -> STILL CRASHES (eliminated); winver.o removed from the link entirely -> STILL CRASHES (eliminated); CXXFLAGS identical (-g -O2); entry point BOTH resolve to dll_entry with byte-identical prologue; section table same 20 sections, same ImageBase, same subsystem.
- CONCLUSION: every measurable input matches and the output still crashes, so the only remaining variable is the ACTUAL LINK COMMAND used at 12:24, which exists nowhere in the tree, the logs, or any script. This is no longer a hypothesis about reproducibility - the failure to rebuild while matching every measurable input IS the demonstration.
- NON-DIFFERENCE I ALMOST REPORTED AS A DEFECT: the two DLLs' entry addresses differ (0x1800d29b0 vs 0x180073750). Both are dll_entry; the addresses differ only because object layout shifted. Recorded because a difference is not a defect.
- STATUS: the two source fixes are READY TO LAND (patch state/arm64-fork-and-argv-fixes.patch, compiles clean under -Werror, codegen verified). Their validation in a real build is BLOCKED by the reproducibility defects above, which are pre-existing and not introduced by this work.
- ** DEFECT 5 SIZED - the undefined __MSYS__ is programme-wide, not one symbol (supervisor 290c9aaf, 2026-09-03 ~17:55).** 38 occurrences across 18 files, and __MSYS__ is DEFINED NOWHERE in the tree (no header, Makefile.am, configure.ac or config.h) - it can only come from the build command line, which the recorded command does not supply.
- Blast radius: environ.cc 10 sites (setenv("MSYS",..), MSYSTEM=, DIFFERENT environment index tables 0,2,3,5,7,9 and marker string "CMPSTW"); fhandler/pty.cc 6; mm/cygheap.cc 3; lib/cygwin_crt0.c + crt0.c + cygwin_attach_dll.c + _cygwin_crt0_common.cc 6 (process startup); dtable.cc (recognising msys- prefixed DLLs); syslog.cc, uname.cc, hookapi.cc, pipe.cc, version.h and others.
- MOST SIGNIFICANT: mm/cygheap.cc gates init_installation_root(). With __MSYS__ the code backs up TWO folders ("we have all stuff in usr subfolder"); without it, one - and "#ifndef __MSYS__" additionally WRITES REGISTRY KEYS that the MSYS build does not. That function runs inside setup_cygheap(), the same startup path as the fork defect.
- CONSEQUENCE: a rebuild from the documented command yields a runtime that computes the wrong installation root, writes unwanted registry state, omits MSYSTEM/MSYS, uses different environment index tables, mishandles PTYs, fails to recognise msys- DLLs, and exports cygwin_dll_init instead of msys_dll_init - while compiling and linking CLEANLY with no diagnostic.
- ** MY __MSYS__ EXPLANATION FOR THE REBUILD CRASH IS REFUTED.** I proposed that objects rebuilt by make without -D__MSYS__ caused the 0xC0000005. Tested by hashing EVERY object in both trees (not archive member names, which was the earlier gap): 273 vs 275 objects, content differs in ./dcrt0.o ONLY (as intended); sigfe.o matches EXACTLY; extras are only fenv_extern_aarch64.o and winver.o. Nothing was rebuilt without __MSYS__. Hypothesis discarded. Defect 5 itself is unaffected - it remains a real silent hazard, it just did not cause this crash.
- The two fenv objects are EQUIVALENT: original fenv_aarch64.o (4,747 B) and my fenv_extern_aarch64.o (4,751 B) share the same DWARF producer, the same source path (math/aarch64/fenv_extern_aarch64.c) and the same symbol lists; the 4-byte delta is the embedded object filename.
- ** FALSE ALARM RAISED AND RETRACTED WITHIN THE SAME CYCLE:** I observed that fenv_extern_aarch64.c appeared to have VANISHED from the shared runtime tree and began treating it as evidence another session was mutating shared state. WRONG - the file is present (1,570 B, dated 08:17); my earlier ls output had been truncated by my own "head -45". Verified properly with an explicit negative test: find <shared runtime> -newermt '-2 hours' -type f -> COUNT 0. Nothing in the shared runtime source has been modified in two hours.
- PATTERN NAMED (third instance today, with the fenv "no source anywhere" retraction and the entry-point "difference"): I have repeatedly treated absence in a truncated or narrowly-scoped listing as absence in fact. The countermeasure that works is running an explicit negative test rather than trusting a listing.
- CRASH REMAINS UNEXPLAINED: every object matches by content, every generated input matches, sigfe.o and winver.o are each independently eliminated, the fenv objects are equivalent, CXXFLAGS matches, and both images enter at dll_entry with byte-identical prologues - yet the rebuild still crashes. No measurable variable remains; the only untested one is the unrecorded 12:24 link command. Supervisor has stopped spending cycles on it.
- ** NEW DEFECT - exec() IS BROKEN ON ARM64 (supervisor 290c9aaf, 2026-09-03 ~18:55).** Third defect, distinct from fork and argv. P3's "signal/TLS/fork/exec fixtures" had NO exec coverage; the existing p3 fixture tests malloc file setjmp sigsetjmp tls signal fork only. Built p4exec.c to close that gap; exec FAILS.
- DIFFERENTIAL across three DLLs, same fixture: unfixed 20AD86BA -> "dofork: child died 0xC0000005, errno 11", i.e. the KNOWN fork bug blocks before exec is ever reached. Both-fixes 18CE46B0 -> fork SUCCEEDS, then "child_copy: cygheap read copy failed, 0x800000000..0x800025900, done 0, Win32 error 6" followed by "fatal error - couldn't create signal pipe, Win32 error 5"; child never runs user code and the exit status returns 0 instead of the sentinel 42.
- THE FIXES MOVED THE FAILURE FROM fork TO exec - which is what a real fix does. exec was UNREACHABLE on the unfixed build, which is why this defect had never been observed.
- Win32 error 6 is ERROR_INVALID_HANDLE; Win32 error 5 is ERROR_ACCESS_DENIED. The failing copy range STARTS AT 0x800000000, the same cygheap base implicated in the fork bug - same memory region, different mechanism.
- CONTROL: fork, signal and malloc all still PASS in the same directory with the same DLL, so this is exec-specific and NOT a regression introduced by the fork/argv fixes.
- NOT ROOT-CAUSED and no fix proposed. Whether it shares a cause with the fork bug (both involve 0x800000000) is UNVERIFIED SPECULATION, flagged as such rather than asserted.
- INCIDENTAL IN-THE-WILD CONFIRMATION OF THE ARGV DEFECT: on the unfixed DLL the runtime printed its own process name as "pp4exec" and received the argument "fork__exec" - the character-duplication bug appearing spontaneously in unrelated runtime output. Both are clean on the fixed DLL.
- FIXTURE DELIVERED: p4exec.c / p4exec.exe (70e31eee...), four cases - execl_self, fork_exec, spawn_wait (3 cycles), plus an internal child mode. Default test is fork_exec so it runs correctly even on builds where argv is still corrupted.
- UPDATED P3 PICTURE: malloc/file/setjmp/sigsetjmp/tls/signal PASS; fork PASS with the headroom fix (was FAIL); exec FAIL - new, unowned, not root-caused.
- ** exec: HANDLE INHERITANCE WORKS ON WoA - platform ELIMINATED by measurement (supervisor 290c9aaf, 2026-09-03 ~19:40).** Reproduced the runtime's exact pattern in pure Win32 with NO MSYS2 runtime: DuplicateHandle(self,self,self,&parent,perms,TRUE,0) using the EXACT exec permission set (PROCESS_QUERY_LIMITED_INFORMATION|PROCESS_VM_READ|PROCESS_VM_OPERATION|SYNCHRONIZE = 0x101018, deliberately WITHOUT PROCESS_DUP_HANDLE which is fork-only), then CreateProcessW(bInheritHandles=TRUE) as spawn.cc:660 does. Child inherited handle 0x788, GetHandleInformation flags=0x1, GetProcessId resolved to the parent's pid, and ReadProcessMemory succeeded 64/64 returning the parent's data - the IDENTICAL operation child_copy fails at. Harness: hparent.ps1 / hchild.ps1.
- exec: THE FAILURE IS DETERMINISTIC, NOT A RACE - 6/6 runs fail identically. I had a plausible race hypothesis (for _CH_EXEC the runtime passes need_subproc_ready=false so there is no subproc_ready synchronisation, and in _P_OVERLAY the parent is replaced) and DISCARDED IT on the measurement rather than passing it on.
- exec: child_info TRANSFER INTEGRITY ELIMINATED. get_cygwin_startup_info (dcrt0.cc:525-575) validates intro, magic (CHILD_INFO_MAGIC ^ MSYS2_RUNTIME_COMMIT_HEX), res->cb against sizeof(child_info_spawn), and fhandler_union_cb. NONE of those diagnostics ("proc size", "proc magic", "fhandler size") appear in the failure output, so the structure arrives intact and the handle field is what the parent wrote. child_info is passed via si.lpReserved2 / si.cbReserved2 (spawn.cc:556-557, fork.cc:326-327).
- ** NEW MINOR DEFECT - child_copy REPORTS A STALE WIN32 ERROR ON THE SHORT-COUNT PATH (fork.cc:753-767).** The code is "if (!res || todo != done) { if (!res) __seterrno (); ... system_printf(... %E) }". When ReadProcessMemory returns TRUE but a SHORT COUNT, __seterrno() is skipped and %E prints a stale error from an earlier unrelated call. Our exec failure reports "done 0" - zero bytes - which is exactly the shape a short-count-with-success would produce. THEREFORE the reported "Win32 error 6 = ERROR_INVALID_HANDLE" MAY NOT DESCRIBE THE ACTUAL FAILURE, and it is what sent this investigation down the handle-inheritance path for two cycles. Fix is small, safe and architecture-neutral: set or clear the error on BOTH branches. Affects fork and exec diagnostics alike.
- exec REMAINING LEAD (flagged, NOT asserted): child_copy reads INTO THE SAME ADDRESS it reads from - ReadProcessMemory(hp, here, here, todo, &done). The failing range 0x800000000..0x800025900 begins at the very base where this programme already established StackBase == CYGHEAP_STORAGE_LOW on ARM64 - the same collision behind the fork defect. cygheap.cc:95-98 does a VirtualAlloc(MEM_COMMIT) immediately before the copy. A zero-length copy is CONSISTENT WITH the destination region not being available in the child, but this is untested.
- SUPERVISOR CORRECTION: I previously stated that progressing exec "means instrumenting the runtime and rebuilding". That was WRONG and would have parked a tractable investigation behind a blocker that did not apply - two useful eliminations came from a ~20-line pure-Win32 probe requiring no rebuild. Same pattern as the day's other errors: over-scoping the prerequisite instead of asking what the smallest decisive test was.
- ** RETRACTION - the exec "destination region not committed" lead is REFUTED (supervisor 290c9aaf, 2026-09-03 ~20:25).** I reported an ordering difference (exec'd child FREE at the copy, COMMIT only afterwards) as an evidence-backed lead. It is WRONG. cygheap.cc:92-103 assigns cygheap from VirtualAlloc and passes it straight to child_copy as the "low" argument, and the failure message PRINTS that argument: "0x800000000..0x800025A60". low != NULL therefore VirtualAlloc SUCCEEDED and the destination WAS committed. The refuting evidence was inside the runtime's own error message, which I had been reading for four cycles.
- ROOT OF THAT ERROR: I inferred a timeline from where a printf appeared in INTERLEAVED CONSOLE OUTPUT relative to my debug-event samples. Console output ordering is NOT a timeline - stdout buffering means the message can appear anywhere relative to sampling. Countermeasure: read what the program already reports before instrumenting to discover it.
- ** NEW LATENT DEFECT - VirtualAlloc RETURN VALUES ARE UNCHECKED (cygheap.cc:92-100).** Both the MEM_RESERVE/PAGE_NOACCESS and MEM_COMMIT/PAGE_READWRITE calls assign to cygheap with NO "if (!cygheap)" before child_copy consumes it. If either failed, the runtime would copy into a NULL range instead of reporting a diagnosable error. Did NOT fire in this failure (the address printed non-NULL), but it is latent, architecture-neutral and cheap to fix. Record alongside the child_copy stale-error defect.
- exec STATUS AFTER SIX ELIMINATIONS: handle inheritance (measured on WoA with the exact exec permission set), the permission set itself, bInheritHandles, child_info transfer integrity, source region mapped and PAGE_READWRITE in the parent at the moment of the copy (measured under a debugger), and destination region committed (proven by the runtime's own printed argument). Not a race (deterministic 6/6). EVERY INPUT TO ReadProcessMemory IS VERIFIED GOOD AND IT STILL RETURNS done 0.
- CONSEQUENCE: the reported "Win32 error 6" is untrustworthy (see the child_copy stale-error defect) and the TRUE error is unknown. Until someone captures the actual GetLastError at the failure - one instrumented line, or a breakpoint on the ReadProcessMemory return - further reasoning from the visible symptoms is unsound. The supervisor has now demonstrated that twice by advancing two leads that were later refuted.
- ** exec ROOT CAUSE ISOLATED BY DIRECT MEASUREMENT: the parent handle is NOT INHERITED into the exec'd child (supervisor 290c9aaf, 2026-09-03 ~21:05).** Breakpointed the `bl ReadProcessMemory` inside child_copy (0x1800cadf4, RVA 0x8adf4) in every process, read x0 = hProcess, then from the DEBUGGER attempted DuplicateHandle(targetProcess, value, self, ...) to test that handle's validity from outside. Result: FORK child pid=1356 hProcess=0x204 -> DuplicateHandle OK, handle VALID, refers to pid=4152 (its parent). EXEC child pid=16364 hProcess=0x19c -> DuplicateHandle FAILED err=6, handle INVALID in that process. Same run, same fixture, same DLL.
- The exec'd child's child_info carries a PLAUSIBLE, NON-ZERO handle value (0x19c, not garbage) that simply does not exist in its handle table. So child_info transfers the VALUE correctly (its intro/magic/cb validation passes) but the HANDLE OBJECT is not inherited on the exec path, while it is on the fork path.
- CORROBORATING MEASUREMENT (separate probe, rpm2.ps1): at the ReadProcessMemory RETURN, fork child returns x0=1 (success) and exec child returns x0=0 (failure). Because res==FALSE, __seterrno() IS called, therefore the reported ERROR_INVALID_HANDLE is the GENUINE error, not a stale one.
- ** SUPERVISOR CORRECTION - "stale error" was NOT the explanation here.** I raised the child_copy stale-error defect two cycles earlier and leaned on it as the leading explanation. The defect is real code-quality issue but it did NOT fire in this failure. Measured, not argued.
- ** SUPERVISOR CORRECTION - "every input to ReadProcessMemory is verified good" was WRONG.** I had proved handle inheritance works AS A MECHANISM on WoA with the exact exec permission set, then generalised that to "the handle is fine". Demonstrating a mechanism works in isolation is NOT demonstrating it was applied correctly in the failing case - the same lesson recorded earlier today in a different context.
- ** RETRACTED - ".text instrumentation breaks fork" is UNSUPPORTED.** I reported that planting a breakpoint in msys-2.0.dll .text appeared to stop fork, and flagged it as a debugging constraint worth knowing. A CONTROL RUN WITH NO WRITE AT ALL HUNG IDENTICALLY. The true cause was my own harness passing DBG_CONTINUE for every exception, which re-executes faulting instructions forever; the harness that worked (execmem2.ps1) passed DBG_EXCEPTION_NOT_HANDLED for exceptions that were not its own. The breakpoint route is NOT blocked - and once fixed, it produced the two measurements above.
- SUPERVISOR PATTERN, RECORDED FOR WEIGHTING: five proposed MECHANISMS today were each refuted by a control I ran after reporting (TEB/ASLR identity, fenv source absence, destination-not-committed, entry-point difference, .text-breaks-fork). The ELIMINATIONS and MEASUREMENTS have held. Corrective adopted: run the negative control BEFORE reporting a mechanism, and confine reporting to eliminations and measurements.
- HARNESSES DELIVERED (reusable, no rebuild required): rpm2.ps1 (captures the ReadProcessMemory boolean in every process), hval.ps1 (captures the handle value and tests its validity from outside), execmem2.ps1 (per-event region state), hparent.ps1/hchild.ps1 (pure-Win32 inheritance harness), p4exec.c (the exec fixture).
- ** CORRECTION TO THE PRECEDING ENTRY - "the handle object is NOT INHERITED on the exec path" is SUPERSEDED AND WRONG (supervisor 290c9aaf, 2026-09-03 ~21:20).** Inheritance DOES work on the exec path: a valid process handle to the parent IS present in the exec'd child. The actual defect is that child_info.parent RECORDS A VALUE THAT DOES NOT MATCH the handle the child received. My earlier test checked only the single value child_info names and did not enumerate the child's handle space, so "invalid" was read as "absent".
- MEASURED 3/3, by breakpointing the bl ReadProcessMemory in child_copy to capture x0, then scanning handle values 4..0x600 in that process via DuplicateHandle from the debugger: FORK run A child_info=0x204 / inherited 0x204 -> MATCH; run B 0x20c / 0x20c -> MATCH; run C 0x19c / 0x19c -> MATCH. EXEC run A child_info=0x1a8 INVALID / inherited 0x200 -> MISMATCH; run B 0x1a0 INVALID / 0x208 -> MISMATCH; run C 0x1a4 INVALID / 0x198 -> MISMATCH.
- The mismatch is NOT a fixed offset (the real handle is sometimes higher, sometimes lower than the recorded value), so it is a WRONG VALUE rather than a shifted one. The exec'd child holds a normal handle population (57 valid in the scanned range vs 59 in the fork child), confirming inheritance as a whole is functioning.
- ** NET ISOLATION: the exec defect is that whatever populates child_info.parent for _CH_EXEC writes a value that does not correspond to the handle actually inherited by the child.** Everything around it is eliminated by measurement: handle inheritance mechanism, permission set, bInheritHandles, child_info structural integrity, source region mapped and RW in the parent, destination region committed, not a race (deterministic 6/6), and the reported ERROR_INVALID_HANDLE is genuine (res==FALSE captured at the call return, so __seterrno did run).
- SUPERVISOR PATTERN, SECOND INSTANCE OF A MEASUREMENT-BASED ERROR (not merely a hypothesis): both times the cause was measuring the thing asked about WITHOUT measuring the surrounding space. Testing one handle said "invalid"; enumerating the space showed the correct handle was present all along. Corrective added to the standing list: when a measurement says X is absent, check whether X exists ELSEWHERE before concluding absence.
- HARNESS: hscan.ps1 (captures child_copy's hProcess argument, tests its validity externally, and enumerates the process's real handle population). Reproduces on demand, no rebuild required.
- ** exec ISOLATION COMPLETED - the handle VALUE is preserved across fork inheritance but NOT across exec inheritance (supervisor 290c9aaf, 2026-09-03 ~22:00).** Extended the probe to test the recorded child_info.parent value against EVERY live process, not only the child that uses it. This closes the remaining question: is the parent-side value wrong, or is the transfer wrong?
- MEASURED, one run, both paths: FORK - child pid=9128 records 0x204; 0x204 is VALID IN THE CHILD and refers to pid23748; and 0x204 IS ALSO valid in pid23748 (the parent) as a PROCESS handle to ITSELF. Value preserved end to end. EXEC - child pid=11948 records 0x1a8; 0x1a8 is INVALID IN THE CHILD; the child actually holds its parent handle at 0x200 -> pid9128; and 0x1a8 IS valid in pid9128 (the exec'ing parent) as a PROCESS handle to ITSELF.
- ** THEREFORE THE PARENT SIDE IS CORRECT.** The exec'ing parent really does hold an inheritable process handle to itself at 0x1a8 and really does record 0x1a8 in child_info. The child really does receive an equivalent handle. The defect is that THE HANDLE ARRIVES AT A DIFFERENT VALUE (0x200) THAN THE ONE RECORDED (0x1a8), so the recorded value is meaningless in the child's handle table. On the fork path the value is preserved and the same code works.
- This supersedes nothing but sharpens the previous entry: it is not that child_info records a "wrong value" in the sense of a computation error - the value is correct FOR THE PARENT. The transfer does not preserve it on the exec path.
- Harness: hboth.ps1 (captures child_copy's hProcess argument, tests validity in the using process, enumerates that process's real handle population, and cross-checks the recorded value against every other live process). No rebuild required.
- Supervisor note: I had stated I had "no further non-mutating measurement queued". That was wrong - this measurement was available the whole time and required only widening the existing probe from one process to all of them. Same failure mode as the two earlier measurement errors: measuring the thing asked about without measuring the surrounding space.
- ** exec: FORK IS NOT INVOLVED - direct execl() with no fork anywhere reproduces the failure identically (supervisor 290c9aaf, 2026-09-03 ~22:12).** Ran the fixture in execl_self mode (the process execs ITSELF; no fork() is ever called) under the same harness. Result: root pid=24484 execs -> child pid=6088; child_info records 0x20c which IS valid in pid24484 as a PROCESS handle to itself; the child actually holds its parent handle at 0x204; child_copy fails with the identical "cygheap read copy failed ... done 0, Win32 error 6". Only ONE child_copy hit occurs (no fork child), and it fails. This ELIMINATES any interaction with fork, with the fork/argv fixes, or with a fork-then-exec sequence.
- ** THE DISCREPANCY IS CONSISTENTLY SIGNED AND SMALL.** Direct-exec runs: recorded 0x208 / actual 0x200 / delta +8; recorded 0x214 / actual 0x208 / delta +12; recorded 0x204 / actual 0x1fc / delta +8. The RECORDED value is always HIGHER than the handle the child actually receives, by 8 or 12 - i.e. two or three handle slots (Windows handle values are multiples of 4). On the FORK path the delta is exactly 0 in every run measured.
- NET CHARACTERISATION FOR AN OWNER: on the exec path the parent records a self-handle value that is consistently 2-3 handle slots ABOVE the value at which the child receives the inherited handle; on the fork path the two agree exactly. Everything else around the operation is eliminated by measurement (handle inheritance mechanism on WoA, permission set, bInheritHandles, child_info structural integrity, source region mapped and PAGE_READWRITE, destination region committed, not a race, reported ERROR_INVALID_HANDLE genuine).
- Harness: hdirect.ps1 (same as hboth.ps1 but launches the fixture in execl_self mode so fork never occurs). No rebuild required. Reproduces on demand.
- MEASURED: Windows DOES preserve inherited handle values across CreateProcess, including to a GENUINELY DIFFERENT image. Parent values 0x348/0x34c/0x350/0x354; same-image child 4/4 VALID; different-image child 4/4 VALID.
- Measured on THIS ARM64 host with native ARM64 processes, using a purpose-built probe independent of any fixture, harness or debugger.
- REFUTES the propagating premise that "the exec child is a different image with a different handle-allocation history, so slots need not coincide". Different image, same slots.
- SCOPE LIMIT: establishes the OS-level guarantee for direct CreateProcess inheritance ONLY. Says nothing about the Cygwin exec transfer path.
- BOUNDED OBSERVATION, not a mechanism: handle values are multiples of 4, so the reported +8/+12 deltas are exactly 2 and 3 handle slots, with the recorded value always HIGHER than the received one.
- SELF-CAUGHT: my handle-preservation probe had NO NEGATIVE CONTROL. Re-ran with inheritance-OFF arm plus an IDENTITY marker (parent signals exactly one event).
- NEG arm bInheritHandles=FALSE: 4/4 VALID but signalled index 1 = the child OWN object. TEST arm bInheritHandles=TRUE: 4/4 VALID, signalled index 2, MATCHES PARENT.
- SO "4/4 values valid" HAS NO SPECIFICITY - it fires identically with inheritance off. Same failure class I catalogued in others: a detector returning the same answer in both arms.
- CONCLUSION SURVIVES ON BETTER EVIDENCE: Windows DOES preserve inherited handle values including into a genuinely different image, proven by IDENTITY not validity. ARM64 host, native ARM64 processes.
- WARNING RECORDED: anyone re-running my ORIGINAL probe design would get 4/4 in both arms and correctly conclude it proves nothing.
- MEASUREMENT SUGGESTION (not a mechanism): comparing handle VALUES across exec must confirm SAME KERNEL OBJECT via a signalled-state marker; validity cannot distinguish an inherited handle from a coincidental one in the same slot.
- DEFECT NAMED AND VERIFIED: str x4,[sp,#24] at RVA 0x6CD8 in _dll_crt0. I decoded both raw words independently - 0xaa1203e4 = mov x4,x18 and 0xf9000fe4 = str x4,[sp,#24]. BOTH MATCH.
- Arithmetic closes on MY measured StackBase: 0x800000000 - 16 = 0x7fffffff0; [sp,#24] = 0x800000008 = cygheap+8. IDENTICAL. Reach 32 bytes against 16 available.
- NEW ERROR CLASS (8th): a CORRECT architectural fact applied to the documented purpose of an instruction that had TWO purposes. x86 subq $32 also supplied headroom; harmless to remove on x86_64 (locals at negative offsets from rbp), fatal on AArch64 (spills at positive offsets from sp).
- MY WORST ERROR CLOSED OUT: the withdrawn geometric exclusion would have ruled out head-writing mechanisms. THE TRUE ANSWER IS A HEAD-WRITE. Withdrawing it re-opened the door to the correct answer.
- GOVERNANCE ESCALATION RESOLVED AND VERIFIED: commit 9d951e2d3e96 on crutkas-link-arm64-msys-runtime-dll, pushed 05:14Z, places the retraction ADJACENT TO THE LINK LINE in Makefile.am and carries the MEASUREMENTS AND CONTROLS, not a conclusion. All figures check against my records. Four tracked PR heads unchanged - it landed on a separate branch.
- SCOPE CAVEAT ON THAT COMMIT: it says "Windows on Arm MANDATES DYNAMIC_BASE" but all six of my DLLs were MICROSOFT SYSTEM DLLs. My attempt to widen it to non-system DLLs is VOID (pristine control failed). Operative instruction sound; universal phrasing rests on system DLLs plus c63ab774 fixedbase observation.
- SEVENTH MISATTRIBUTION: c63ab774 credited me with the lead, the -D__MSYS__ finding and the 32-vs-16 figure. None mine; 290c9aaf again. Corrected on the grounds that it distorts the independence of their corroboration.
- ** LINK SESSION de02421a FIXED THE cygwin.sc.in TEMPLATE (its finding, recorded here for continuity; observed by supervisor 290c9aaf at 2026-09-03 22:11).** It changed "#ifdef __x86_64__" to "#if defined(__x86_64__) || defined(__aarch64__)" in two places and documented the mechanism inline: with 4-byte ctor/dtor markers an 8-byte read STRADDLES the ctor terminator LONG(0) and the dtor head marker LONG(-1), yielding exactly 0xFFFFFFFF00000000, which is then called as a function pointer. Its measurements: pre-fix __DTOR_LIST__ at 0x180217EFC (4 mod 8, misaligned), post-fix at 0x180217F00. It also made the .xdata rule explicit for aarch64, noting that orphan-section placement "happened to work ... but it was luck, not design" and that __unwind_single_frame depends on it via RtlLookupFunctionEntry/RtlVirtualUnwind.
- ** SUPERVISOR CORRECTION - I RETRACTED THIS MECHANISM EARLIER TODAY AND THE RETRACTION WAS TOO BROAD.** 0xFFFFFFFF00000000 is the exact fault address I captured earlier and attributed to a 32-bit ctor-list terminator. Precisely: my MECHANISM was CORRECT (independently reached by de02421a with the same fault signature); my FIX was WRONG (12-byte markers) and quarantining it as DEFECTIVE-w-ctorfix-DO-NOT-USE was right; and my retraction correctly observed that the GENERATED cygwin.sc already handled __aarch64__ - which is why the shipped DLL works - but I wrongly generalised that to dismiss the mechanism itself. The TEMPLATE cygwin.sc.in was still x86_64-only (sealed copies remain 5299 B / eacf11af...). Discarding a mechanism because one instance of it was already handled is not sound; the template and the generated file had to be distinguished.
- ** TESTED WHETHER THE ctor DEFECT EXPLAINS THE SUPERVISOR'S REBUILD CRASH: NEGATIVE.** Direct comparison of both images: WORKING 12:24 DLL __CTOR_LIST__=0x1d6f20 __DTOR_LIST__=0x1d6fc0; the crashing rebuild has BYTE-IDENTICAL addresses, both 8-byte aligned (0x1d6fc0 mod 8 = 0). The rebuild inherited the already-correct generated cygwin.sc, so ctor markers are ELIMINATED as a cause of the 0xC0000005. That crash remains unexplained and still turns on the unrecorded 12:24 link command.
- PROGRAMME IMPLICATION: until this fix, the working DLL depended on a GENERATED file being correct while its SOURCE TEMPLATE was not - the same single-copy fragility the programme has flagged repeatedly. Anyone regenerating cygwin.sc from the template before 22:11 would have produced a DLL that crashes inside DllMain before any user code, and the linker validates nothing here: 4-byte, 8-byte and deliberately wrong 12-byte markers all link cleanly.
- ** BOTH FIXES ARE NOW VALIDATED IN A REAL FROM-SOURCE BUILD (supervisor 290c9aaf verifying link session de02421a's work, 2026-09-03 22:44).** de02421a applied the fork fix at 22:23 (DLL 54e464d0) and the argv fix at 22:44 (DLL 90bfb483), compiling and linking both itself. Every prior confirmation by the supervisor was a guarded BINARY PATCH; this is the first validation in a genuinely rebuilt runtime, which closes the validation gap that the build-reproducibility defects had made impossible.
- MEASURED against DLL 90bfb483ae44aea2: P3 = malloc PASS, file PASS, setjmp PASS, sigsetjmp PASS, tls PASS, signal PASS, fork PASS -> 7/7 against a 5/7 baseline. argv = 'abcdefg' -> 'abcdefg' OK; 'abcdefghijklmnop' -> 'abcdefghijklmnop' OK; 'abcdefghijklmnopqrstuvwx' -> 'abcdefghijklmnopqrstuvwx' OK. Command-line corruption is GONE.
- de02421a's fork fix uses "sub sp, sp, #64" - more margin than the supervisor's measured 32-byte requirement and more than the recommended 32, which is the safer direction. Its inline comment records the mechanism independently: AArch64 addresses frame slots at POSITIVE offsets from sp, and the measured TEB spill "str x4,[sp,#24]" landed on cygheap->chain.
- INTERMEDIATE STATE WORTH RECORDING: at 22:24 the fork fix alone gave P3 7/7 while argv was still CORRUPT ('abcdefg' -> 'abccdefg'). sigsetjmp passed in that build only because the argv corruption position tracks TOTAL COMMAND-LINE LENGTH and happened to land outside the argument in that directory. A 7/7 P3 therefore did NOT imply argv was fixed, and was flagged as such at the time rather than being read as a clean sweep.
- ** [RETRACTED 2026-09-03 23:05 by supervisor 290c9aaf - THE FOLLOWING ENTRY IS FALSE; see the CORRECTION immediately below. Preserved unaltered for audit.] ** ** exec REMAINS THE ONE OPEN P3 DEFECT, AND ITS FAILURE MODE HAS CHANGED TWICE.** Original: "child_copy: cygheap read copy failed ... done 0, Win32 error 6" with the child dying in cygheap_fixup_in_child. On DLL 54e464d0 (fork fix only): that error is GONE and the child exits 99, which is the fixture's "execl() returned" path - i.e. the cygheap copy now SUCCEEDS and execl itself fails. The supervisor's nine eliminations were measured against the ORIGINAL symptom and MUST NOT be carried forward as if they still apply; exec requires re-characterisation against the current build.
- ** CORRECTION - exec's failure mode NEVER CHANGED; the retracted entry above inverted it (supervisor 290c9aaf, 2026-09-03 23:05).** Re-measured with the SAME fixture across three DLLs: baseline 20ad86ba -> "dofork: forked process died, exit code 0xC0000005, errno 11" with NO child_copy line at all (fork dies first, so exec is NEVER REACHED); 54e464d0 (fork fix) -> child_copy ... Win32 error 6 + signal pipe error 5 + raw status 0xb; 90bfb483 (fork+argv) -> IDENTICAL. child_copy is therefore the symptom that APPEARS ONCE FORK STOPS MASKING IT, not an old symptom that disappeared. Consequences: (1) exec is NOT a regression - it was always broken and simply unreachable, so neither the fork/argv fixes nor de02421a caused it; (2) the nine eliminations recorded at status.md 507 REMAIN VALID and must be carried forward - asserting otherwise caused the main session to re-derive bInheritHandles, the permission set and PROCESS_DUP_HANDLE, all already closed BY MEASUREMENT in pure Win32; (3) status.md 477-519 was CORRECT throughout ("on the unfixed runtime exec was never reachable") - the error existed only in this inbox entry and in supervisor messages, i.e. a verbal report contradicted its own tracker.
- ** P3 FIXTURE DEFECT (mine), found by the main session's independent re-characterisation and worth propagating to every exec test:** p4exec.c decodes wait status with (status >> 8) & 0xff ALONE, which evaluates to 0 for a SIGNAL DEATH, so it prints "child exit code = 0 (expected 42)" for a SIGSEGV (raw status 0xb). Any exec fixture using that decode silently mis-reports signal deaths as exit code 0, and this plausibly produced the false "exit 99 / execl() returned" reading. The fixture needs WIFSIGNALED/WTERMSIG handling before its exec verdicts are trusted. Corroborating detail from the same run: execl does NOT return (no post-exec line ever prints), and the failure reproduces with the OTHER session's binary against the same DLL, so it is not a link artefact. **FIXED AND VERIFIED 2026-09-03 23:12:** p4exec.c now decodes with WS_IFSIGNALED/WS_TERMSIG/WS_IFEXITED at both wait sites; rebuilt (p4exec.exe d459bdfd) and re-run against 90bfb483, which now prints "child KILLED BY SIGNAL 11 (never exited; expected exit 42)" in place of the misleading "child exit code = 0". Decode proved in BOTH directions on a host control: 0x2a00 -> exited/42 (the PASS case still recognised), 0xb -> signaled/SIGSEGV, 0x6300 -> exited/99. That last value is decisive: had execl actually returned and the child run _exit(99), waitpid would have reported 0x6300, but every observed run reports 0xb - so the briefed "exit 99 / execl() returned" state was NEVER observed on any build.
- ** HALF THE SUPERVISOR'S exec ISOLATION IS RETRACTED - validity was inferred as identity (supervisor 290c9aaf self-correcting on the verifier's negative control, 2026-09-03 23:15).** WHAT STANDS: on fork the recorded handle functionally works (ReadProcessMemory through it SUCCEEDS - a functional identity test, not a validity test); on exec the recorded value is genuinely INVALID (tested positively, DuplicateHandle returns err 6). WHAT DOES NOT STAND: the further claim that a VALID parent handle IS present in the exec'd child at 0x200/0x208/0x198, so child_info ''simply names a different one''. state/handle-identity-vs-validity.md settles it with a purpose-built Win32 probe (no MSYS2, no Cygwin, no debugger): with inheritance fully OFF the child still reported a VALID+SIGNALLED handle at an adjacent value, and across shifts of +-4/8/12 from a true inherited handle 5 of 6 arms read VALID+SIGNALLED while being a DIFFERENT kernel object - handle values allocate densely at 4-byte granularity and the child's OWN table populates the same range. The ''57 valid vs 59'' population count carries no identity weight either. WHY IT MATTERS OPERATIONALLY: if the child really holds the inherited handle elsewhere the defect is ''the recorded value is wrong'' and the _CH_EXEC population path is the right target; if it does not, the defect is ''the handle was never inherited'' - different code, different fix. plan.md 0c and status.md 477-525 are both corrected, and the next action is now ORDERED: run the identity discriminator FIRST (parent signals exactly one of several objects, child must report THAT slot signalled, across at least two choices), and only then chase _CH_EXEC. This is the same error pattern already recorded against this supervisor: measuring the thing asked about without measuring the surrounding space.
- Canonical DLL moved 90bfb483 -> 9fcc134e (c63ab774 found its own fork-only A/B had left dcrt0.o STALE - 0 memmove calls against 3 in dcrt0.cc - so a relink from that tree would have silently dropped the argv fix). The supervisor's exec signature was pinned to 90bfb483; the main session re-ran it against 9fcc134e and the signature TRANSFERS EXACTLY (child_copy, error 6, error 5, execl does not return, signal 11, two fixtures). Generalised control worth keeping: a before/after hash proves the binary did not change under you, but it does NOT prove the binary matches the source you believe produced it - pair it with a source-to-object consistency check.
- ** exec ROOT CAUSE NARROWED AND A FIX IS CONFIRMED WORKING (c63ab774 + verifier, 2026-09-03 23:25).** execfix.dll 8ffe979b: direct arm exit 42 (was 2816), forked arm PASS (was SIGSEGV), stderr clean - all on EXIT-CODE criteria, not banners. Identity settled by instrumenting the mint rather than probing validity: CTOR type=1 parent=0x190 minted_in_pid=14816; CHILD got parent=0x190 usable=0 err=6 parent_winpid=14816. The child receives EXACTLY the minted value, minted in the process that calls CreateProcessW, for this spawn - so child_info.parent is populated CORRECTLY and the supervisor's proposed _CH_EXEC population target is DEAD. Surviving fact: a handle for which bInheritHandle=TRUE was REQUESTED at DuplicateHandle, passed with bInheritHandles=TRUE, arrives with the correct numeric value and is ABSENT from the child's handle table. ** PRECISION CORRECTION 2026-09-03 23:36 — the earlier phrasing "a handle DUPLICATED bInheritHandle=TRUE" stated an API REQUEST as an OBSERVED STATE. DuplicateHandle was CALLED with that flag (verified in source), but nobody had called GetHandleInformation on the handle at the CreateProcessW instant. The overclaim was the supervisor's and is corrected here. SUBSEQUENTLY MEASURED — but in a PURE-WIN32 REPLICATION faithful to sigproc.cc:938, NOT in the live runtime: HANDLE_FLAG_INHERIT IS actually set (flags=0x1) under BOTH permission sets, the child receives the handle valid at the same value, and ReadProcessMemory SUCCEEDS (64 bytes, pattern intact) — child_copy's core operation works. So the pure-Win32 call shape does NOT reproduce the failure, and the flag's state on the LIVE runtime's handle at the CreateProcessW instant remains UNMEASURED. ** Also dead: storage-class-implies-lifetime (child_info_spawn () {} is EMPTY; real ctor runs via placement-new in set(), so construction is per-spawn) and fork-ancestry-of-the-minting-process (the direct arm has no fork in its ancestry and fails identically). Reproduced in pure Win32: inheritance IS transitive when every generation passes TRUE, and a second arm reproduced INVALID err6 at the correct transmitted value.
- ** INSTRUMENT TRAP THAT WOULD FALSIFY exec RESULTS, now documented in the fixture (supervisor 290c9aaf, 2026-09-03 23:27).** An exec'd successor's stdout is LOST under redirection while the exit status propagates correctly; reproduced under two independent redirection routes, a timing race refuted by 0/1500/3000 ms giving byte-identical output, and a forked child's output DOES survive. A test that verifies exec by looking for the successor's banner would report ''the child never ran'' on a build where exec demonstrably works. p4exec.c documented this at three sites - its usage line previously said ''expect the child banner'', which encoded the trap in the fixture's own documentation - and every verdict in the file is exit-code based. Compile-checked clean under -Wall. The cited control binary p4exec.exe d459bdfd was deliberately NOT overwritten (built into w-exec3 instead), because the verifier cites it as evidence. Exit-code criterion is also STRONGER than ''exec worked'': 42 only if the sentinel argv arrived intact byte-for-byte, 43 missing, 44 corrupted. Not established whether the lost-stdout behaviour is pre-existing Cygwin behaviour, build-specific, or specific to non-console stdout.
- ** exec ROOT-CAUSED AND FIXED AT SOURCE - and the root cause is the mechanism THIS SUPERVISOR ELIMINATED (c63ab774 found it; supervisor 290c9aaf recording its own miss, 2026-09-03 23:47).** hookapi.cc:43-51 PEHeaderFromHModule switches on FileHeader.Machine against an allowlist containing ONLY IMAGE_FILE_MACHINE_AMD64; an 0xAA64 PE falls to default: return NULL, so hook_or_detect_cygwin returns NULL, set_cygexec(NULL) runs, iscygwin() evaluates FALSE for our own ARM64 binary, and spawn.cc:597 strips HANDLE_FLAG_INHERIT from the parent handle. Live measurement at the CreateProcessW instant: flags=0x0. Clear-site instrumentation: before-clear flags=0x1 iscygwin=0 will_clear=1 -> after-clear flags=0x0. Classification never consults the mount table, so the harness-artefact hypothesis is dead - this is a real port defect. FIX: one added case label, and the get_parent_handle() workaround was REVERTED with the upstream guard restored verbatim - rung14 exec works, rung15 execv and execl both 77, rung19 direct 77, rung18 non-Cygwin 66, full regression clean. The real fix alone is sufficient.
- ** WHY THE SUPERVISOR'S ELIMINATION FAILED, PRECISELY - the same axis error twice.** status.md 'Eliminated by measurement, not argument' listed HANDLE INHERITANCE FIRST, on the strength of a pure-Win32 reproduction using the exact exec permission set 0x101018. That measured the API SHAPE'S CAPABILITY, not the LIVE PATH'S BEHAVIOUR: the shape works and the runtime then threw the bit away. A GENERAL CAPABILITY TEST CANNOT ELIMINATE A SPECIFIC INSTANCE. Separately spawn.cc:597 was dismissed by reading control flow - SetHandleInformation sits inside if (!iscygwin ()), 'not taken when exec'ing a Cygwin program, which is this case' - which reasoned about what iscygwin() SHOULD mean rather than what it EVALUATES TO. An accurately-named predicate returning a wrong value is more dangerous than a misleadingly-named one, because nothing about it looks suspicious; this is a FUNCTION-valued variant of the name-versus-content family whose eight earlier instances tonight all involved files. Both halves of the localisation were needed: the replica returning flags=0x1 EXCLUDED 'Windows anomaly at this API shape', the live path returning flags=0x0 LOCATED the loss inside the runtime, and neither alone reaches spawn.cc:597.
