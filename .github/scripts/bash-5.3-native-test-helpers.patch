--- a/tests/run-all
+++ b/tests/run-all
@@ -25,5 +25,9 @@ trap 'rm -f $BASH_TSTOUT ; exit' 1 2 3 15
 trap 'rm -f $BASH_TSTOUT' 0
 
-PATH=.:$PATH	# just to get recho/zecho/printenv if not run via `make tests'
+if test -n "${NATIVE_BASH_TEST_HELPERS-}"; then
+	PATH=$NATIVE_BASH_TEST_HELPERS:$PATH
+else
+	PATH=.:$PATH	# just to get recho/zecho/printenv if not run via `make tests'
+fi
 export PATH
 
 # unset BASH_ENV only if it is set
