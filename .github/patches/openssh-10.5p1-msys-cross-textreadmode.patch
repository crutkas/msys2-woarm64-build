diff --git a/configure.ac b/configure.ac
index 4b35fc3..82726b1 100644
--- a/configure.ac
+++ b/configure.ac
@@ -789,7 +789,7 @@ case "$host" in
 	AC_DEFINE([DISABLE_WTMP], [1], [Define if you don't want to use wtmp])
 	;;
 *-*-cygwin*)
-	LIBS="$LIBS /usr/lib/textreadmode.o"
+	LIBS="$LIBS ${MSYS_TEXTREADMODE_OBJECT:-/usr/lib/textreadmode.o}"
 	AC_DEFINE([HAVE_CYGWIN], [1], [Define if you are on Cygwin])
 	AC_DEFINE([USE_PIPES], [1], [Use PIPES instead of a socketpair()])
 	AC_DEFINE([NO_UID_RESTORATION_TEST], [1],
diff --git a/configure b/configure
index 011d788..ad8cc1d 100755
--- a/configure
+++ b/configure
@@ -12067,7 +12067,7 @@ printf "%s\n" "#define DISABLE_WTMP 1" >>confdefs.h
 
 	;;
 *-*-cygwin*)
-	LIBS="$LIBS /usr/lib/textreadmode.o"
+	LIBS="$LIBS ${MSYS_TEXTREADMODE_OBJECT:-/usr/lib/textreadmode.o}"
 
 printf "%s\n" "#define HAVE_CYGWIN 1" >>confdefs.h
 
